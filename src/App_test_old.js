// import { render, screen } from '@testing-library/react';
// import App from './App';
// import { within } from "@testing-library/dom";
// import ReactDOM from "react-dom";
// import RQSuperHeroes from "./pages/RQSuperHeroes"

// test('renders learn react link', () => {
//   render(<App />); // which component we want to test
//   const linkElement2 = screen.getByText(/learn react/i); // which element in that element we want to test
//   //screen procura nos elementos do DOM algum elemento que tem no texto dentro "learn react"
//   const linkElement = screen. 
//   expect(linkElement).toBeInTheDocument();//o que esperamos que aconteça com esse elemento
// });

// // it("", ()=>{
// // 
// // })

// // test("it works", ()=>{
// //   const root = document.createElement("div");
// //   ReactDOM.render(<RQSuperHeroes />, root);

// //   const { getByText, getByLabelText } = within(root);

// //   expect(root.querySelector("h2").textContent).toBe("RQSuperHeroes");
// //   expect(root.querySelector("label").textContent).toBe("Hero name:");
// //   expect(root.querySelector("button").textContent).toBe("Add Hero");

// // })
